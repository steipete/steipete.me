---
// Simplified route for catching old URLs (without year)
// Since all posts are now in year-based folders, we just redirect to /blog/

export async function getStaticPaths() {
  // Just a placeholder to demonstrate the structure
  return [
    { params: { old_slug: 'test' }, props: {} }
  ];
}

// Redirect to the blog homepage
return Astro.redirect('/blog/', 301);
---