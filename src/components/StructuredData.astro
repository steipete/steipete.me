---
import { structuredDataJson, type StructuredDataProps } from "@/utils/structuredData";
export type Props = StructuredDataProps;
const json = structuredDataJson(Astro.props);
---

<script is:inline type="application/ld+json" set:html={json} />
