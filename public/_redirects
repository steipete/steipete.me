# Dynamic domain redirect - ensure any domain that isn't steipete.me gets redirected
# This rule is triggered when the host does not match steipete.me
:scheme://:domain/:path* https://steipete.me/:path* 301!  Host=!steipete.me

# Catch-all for ANY domain that isn't steipete.me
/* https://steipete.me/:splat 301!

# Redirect old date-based URLs
/20:year/:month/:day/:slug/* /blog/:slug/ 301!

# Redirect old blog URL pattern
/blog/:year/:month/:day/:slug/ /blog/:slug/ 301!

# Handle missing trailing slashes
/blog/:slug /blog/:slug/ 301!

# Optional: Handle old categories or tags if needed
/categories/:category/* /tags/:category/ 301!
/tag/:tag/ /tags/:tag/ 301!